# File:
# ./fail2ban-data/filter.d/delay-req.conf

[Definition]
ngx_limit_req_zones = [^"]+
failregex = ^{"log":"\s*\[[a-z]+\] \d+#\d+: \*\d+ delaying request, excess: [\d\.]+, by zone \\"(?:%(ngx_limit_req_zones)s)", client: <HOST>,
